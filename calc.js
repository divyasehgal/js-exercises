var calc = {
  sum: (a, b) => {
    return a + b;
  },
  sub: (a, b) => {
    return a - b;
  },
};

var total_sum = calc.sum(1, 2);
var total_sub = calc.sub(2, 3);

console.log(total_sum);
console.log(total_sub);
